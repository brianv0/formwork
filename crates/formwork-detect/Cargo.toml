[package]
name = "formwork-detect"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = "0.2"
